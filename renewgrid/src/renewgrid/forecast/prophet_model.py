"""Prophet model placeholder for later implementation."""

from __future__ import annotations

import pandas as pd


def train_prophet(frame: pd.DataFrame) -> str:
    """Placeholder Prophet trainer that returns status text in Phase 1."""
    _ = frame
    return "prophet_not_enabled_phase1"
